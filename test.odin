package foo

import foo "core:foo"
import "core:slice"

s := "hi\"  hello\" " 

/* multiline 
  /* nested /* and triply so */ */
comment */

v := `this is a multiline
string literal
`
c : int : 100_000

a, b := 4, 5

a := function(4, 3, "hi")
