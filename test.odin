package foo

import foo "core:foo"
import "core:slice"

s := "hi\"  hello\" " 

/* multiline 
comment */

v := `this is a multiline
string literal
`
c : int : 100_000

a, b := 4, 5

a := function(4, 3, "hi")

f :: proc (c: int) -> int {
  return c
}
